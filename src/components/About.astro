<section id="profile" class="section profile-section">
  <div class="shell">
    <div class="eyebrow reveal">Profile / 001</div>
    <div class="profile-grid">
      <div class="portrait reveal">
        <img src="/images/profile.jpg" alt="Portrait of Furkan Bacak" />
        <div class="portrait-code micro">FB / BER / 52°N</div>
      </div>
      <div class="profile-copy reveal">
        <h2>Between the map<br />and the <span>message.</span></h2>
        <p class="lead">
          My work sits where spatial thinking meets developer experience:
          understanding technical systems, then giving people a clear way into them.
        </p>
        <div class="profile-columns">
          <p>
            Trained in Geodesy & Geoinformatics, I think in layers, relationships and scale.
            I bring that mindset to developer tools, API documentation, product narratives
            and community programs.
          </p>
          <p>
            The through-line is enablement: less friction, sharper mental models and
            technology that feels useful before it feels impressive.
          </p>
        </div>
        <div class="capability-list">
          <div><span>01</span><strong>Geospatial systems</strong><i>LiDAR · GIS · spatial data</i></div>
          <div><span>02</span><strong>Developer experience</strong><i>Docs · APIs · enablement</i></div>
          <div><span>03</span><strong>Product narrative</strong><i>Story · strategy · community</i></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .profile-section { position: relative; }
  .profile-grid { display: grid; grid-template-columns: 5fr 7fr; gap: clamp(3rem, 8vw, 8rem); align-items: end; margin-top: 4rem; }
  .portrait { position: relative; height: 690px; overflow: hidden; background: #111; }
  .portrait::after { content: ""; position: absolute; inset: 0; background: linear-gradient(180deg, transparent 60%, rgba(7,9,8,.72)); border: 1px solid rgba(255,255,255,.15); }
  .portrait img { width: 100%; height: 100%; object-fit: cover; object-position: 64% center; filter: saturate(.78) contrast(1.08); }
  .portrait-code { position: absolute; z-index: 2; left: 1.2rem; bottom: 1.1rem; color: var(--acid); }
  .profile-copy h2 { margin: 0 0 2.4rem; font-size: clamp(3rem, 6vw, 6.8rem); line-height: .9; letter-spacing: -.065em; font-weight: 500; }
  .profile-copy h2 span { color: #5d6560; }
  .lead { max-width: 670px; color: #e3e6e0; font-size: clamp(1.2rem, 2vw, 1.55rem); line-height: 1.5; }
  .profile-columns { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; color: var(--muted); line-height: 1.75; margin: 2.5rem 0 3.5rem; }
  .capability-list { border-top: 1px solid rgba(255,255,255,.14); }
  .capability-list div { display: grid; grid-template-columns: 2rem 1fr auto; gap: 1rem; align-items: center; padding: 1.2rem 0; border-bottom: 1px solid rgba(255,255,255,.1); }
  .capability-list span, .capability-list i { color: #68706b; font: normal .62rem var(--font-mono); text-transform: uppercase; }
  .capability-list strong { font-size: .92rem; font-weight: 500; }
  @media (max-width: 850px) { .profile-grid { grid-template-columns: 1fr; } .portrait { height: 520px; } }
  @media (max-width: 600px) { .profile-columns { grid-template-columns: 1fr; } .capability-list div { grid-template-columns: 2rem 1fr; } .capability-list i { grid-column: 2; } }
</style>
